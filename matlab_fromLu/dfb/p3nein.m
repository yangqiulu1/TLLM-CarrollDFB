% set up a suitable initial ELECTRON DENSITY to save computing time
Ne=ones(N,1);  		 %initial fields to transparency.

% if we wish to see how the fields build up then we set Ne to zero;
% This trivial entry is put in as a separate file 
% so that setting of electron density can be altered.